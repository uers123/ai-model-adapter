# 凭据与安全策略

- 只从环境变量或 Windows Credential Manager 读取凭据。
- 配置、日志、报告和导出物只记录凭据名称，不记录值。
- 子进程按最小权限临时注入，结束后清理。
- 写日志和导出前扫描常见 Token、密码、私钥和签名 URL。
- 默认禁止第三方上传；云端、数据上传和发布必须重新授权。
- 发现疑似凭据或隐私数据时立即暂停并标记 `blocked`。
- 清理先生成 `cleanup-plan.json`，再移动到任务回收区；不得自动永久删除原始基线、最佳检查点或正式版本。

